const AppString = ({str}) => {
    //console.log(props);
    const strLen = str.length;

    return (
        <div>
            Длинна строки = {strLen}
        </div>
    )
}

export default AppString;